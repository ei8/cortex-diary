<UserControl x:Class="works.ei8.Cortex.Diary.Port.Adapter.UI.Views.Wpf.Dialogs.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="550" Width="700">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="75" />
            <ColumnDefinition Width="*" />            
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Name="LoadButton" Content="Load" HorizontalAlignment="Right" VerticalAlignment="Top" Width="75"/>
        <TextBox Grid.Column="1" Name="IdentityServerUrl" HorizontalAlignment="Stretch" MinWidth="50" VerticalAlignment="Top" />
        <WebBrowser Grid.Row="1" Grid.ColumnSpan="2" Name="Browser" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />        
    </Grid>
</UserControl>
